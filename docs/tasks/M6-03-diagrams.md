# M6-03 — Диаграммы: C4 + горячий путь + saga

**Майлстоун:** M6 (полировка)
**Зависимости:** архитектура зафиксирована (M2–M4)
**Оценка:** S (0.5 дня)

## Цель
Визуальный ряд для README и собеса (§12): C4 (контекст/контейнеры), диаграмма горячего пути ставки (§6), диаграмма саги сеттлмента (§7).

## Объём работ
- C4 уровня 1–2 (контекст + контейнеры: api, web, postgres, rabbitmq, redis).
- Горячий путь ставки (§6): идемпотентность → CAS → TX+outbox → relay → Pub/Sub → WS, с подписью «тонкого места» (reconciliation).
- Saga сеттлмента (§7): шаги 1–6 + компенсации в обратном порядке.
- Формат Mermaid (рендерится в репо) в `docs/diagrams/`.

## Definition of Done
- Три диаграммы лежат в `docs/diagrams/`, рендерятся, вставлены в README.
- Диаграмма горячего пути отражает шаги §6 точно (включая reconciliation).
